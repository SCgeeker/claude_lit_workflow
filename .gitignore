# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# IDE and editor
.vscode/
.idea/
*.swp
*.swo
*.swn
*~
.DS_Store
.sublime-project
.sublime-workspace

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.hypothesis/
*.cover

# Jupyter Notebook
.ipynb_checkpoints

# Environment variables
.env
.env.local
.env.*.local

# Database and data (large files)
*.db-shm
*.db-wal
knowledge_base/backups/
backups/**/*.db                # Exclude all database backups
knowledge_base/index.db       # Exclude full database (contains embargo papers)
!knowledge_base/index_public.db  # Include public database (examples only)
chroma_db/

# Logs and temporary files
*.log
logs/
*.tmp
temp_*.json
.tmp/

# Large output directories
output/
downloads/
*.zip
*.tar.gz
*.rar

# Archive (personal use)
archive/
# openspec 的已歸檔 change 是規格歷史，需隨 git 同步
!openspec/changes/archive/

# 知識庫資料檔（本地資料，不入版控；備份見 archive/）
knowledge_base/index.db

# docs/ 為本機開發暫存區（session 紀錄、階段報告、停用功能設計稿），不入 repo；
# 僅保留下列策展文檔隨 git 同步。要新增對外文檔時，在此以 !docs/<檔名> 明確納入。
docs/*
!docs/CLAUDE.md
!docs/TROUBLESHOOTING.md

# 本機檔案
.env_bak
*.code-workspace

# System files
Thumbs.db
.cache/
.thumbnails/

# Obsidian
.obsidian/
*.md.backup

# Language models and embeddings (too large)
*.bin
*.model
*.pkl

# User-specific files
.vscode/settings.json
.idea/workspace.xml

# Optional sensitive files
.env.production
.secrets/

# Note: Keep index_public.db in git (public examples for demonstration)
# Note: Exclude index.db from git (contains embargo research papers)
# Note: Exclude output/, archive/, knowledge_base/papers/ (personal use)

# Paper analysis notes (ensure this is at the end)
**/papers/*.md

# Claude Code 本地設定
.claude/settings.local.json
